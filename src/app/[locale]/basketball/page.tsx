import LiveBasketballClient from "@/components/LiveBasketballClient";

export const metadata = {
  title: "Live NBA Scores | ScoreDeck",
  description: "Real-time basketball scores and live updates.",
};

export default function LiveBasketballPage() {
  return <LiveBasketballClient />;
}
